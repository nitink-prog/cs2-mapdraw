# Agent Guidance

- When implementing or changing UI features, support both desktop and mobile layouts.
- Treat `768px` horizontal width and below as the mobile mode unless the product requirements explicitly change that breakpoint.
- Verify that controls remain reachable and usable in both modes, especially map selection, drawing tools, color selection, clear actions, undo/redo, zoom, and Konva stage sizing.
- Preserve nearby `@ink:*` context comments. Update them when behavior changes so future agents can trust them.

## Feature inventory (do not regress without explicit product decision)

**Contract for agents:** The table below is part of the repo’s behavioral spec. When you add, remove, or materially change a user-facing capability, **update this table in the same PR/change** (feature name, intent, and couplings). Before merging refactors or “small” fixes, skim the table and grep the listed files so you do not drop intent (e.g. hit-testing rules, layer ordering, history semantics).


| Feature                                       | Intent / user value                                                                                                                                                                          | Dependencies & couplings                                                                                                                                                                                                                                                             |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Map workspace & catalog**                   | Pick a CS2 map; load its radar and related assets into a drawable 1024×1024 map space.                                                                                                       | `[src/mapCatalog.ts](src/mapCatalog.ts)` (map ids, `getPublicAssetUrl`, asset paths); `[src/App.tsx](src/App.tsx)` (`MAP_WORLD_SIZE`, `selectedMapId`, `useLoadedImage`).                                                                                                            |
| **Map metadata (grenade scale)**              | Correct in-world radii for grenade overlays from `meta.json5` `resolution` (game units per radar pixel). Without metadata, radii are omitted but markers remain.                             | `[src/mapMetadata.ts](src/mapMetadata.ts)` (`useMapMetadata`); `[src/grenadeEffects.ts](src/grenadeEffects.ts)` (`@ink:contract` on resolution); rendering branch in `[src/App.tsx](src/App.tsx)` when `radius === null`.                                                            |
| **Ink drawing (brush)**                       | Draw freehand callouts; **T** vs **CT** color via **left** vs **right mouse**; touch uses the active brush color.                                                                            | `[src/App.tsx](src/App.tsx)`: `handlePointerDown` / `handlePointerMove`, `drawingLayerRef`, `isDrawingRef`, `activeLineRef` (hot path off React state); dedicated top `Layer` with ref; ink `Line`s in annotation layer use `listening={false}` so hits pass through strokes.        |
| **Grenade placement**                         | Place smoke / flash / molotov markers on the map with a single tap (utility tool mode).                                                                                                      | `[src/App.tsx](src/App.tsx)` `addGrenadeMarker`, `pushHistoryAction` `add`; `[src/grenadeEffects.ts](src/grenadeEffects.ts)` symbols, colors, help copy.                                                                                                                             |
| **Grenade overlap & hit policy**              | Users may **ink** and **place grenades** anywhere in the modeled blast area of an existing marker **except** on the **center F/S/M handle**; only the handle blocks placement and ink start. | `[src/App.tsx](src/App.tsx)`: `isGrenadeDragHandlePointerEvent` / `pointerEventHitsNamedSubtree`; Konva `Group` `name="grenade-drag-handle"`; effect visuals `listening={false}`; **do not** reintroduce full-AOI `cancelBubble` or footprint-only guards that block Stage handlers. |
| **Grenade drag & move history**               | Reposition markers by dragging **only** the center handle; persists via undo/redo-friendly `moveGrenade` history.                                                                            | `[src/App.tsx](src/App.tsx)`: nested `Group` drag, absorb handle delta into parent position, `draggingGrenadeId` / `draggingGrenadePosition`, `applyHistoryAction` / `revertHistoryAction` for `moveGrenade`.                                                                        |
| **Ink vs grenade drag while ink tool active** | With **ink** selected, grabbing the **grenade handle** drags the grenade; **does not** start a stroke.                                                                                       | `[src/App.tsx](src/App.tsx)`: ink branch must skip when `isGrenadeDragHandlePointerEvent` so Stage does not open a new `Line`.                                                                                                                                                       |
| **Flash: radial vs ray-cast overlay**         | Toggle LOS-style flash exposure vs simple radial gradient; ray-cast honors radar walls (approximate).                                                                                        | `[src/App.tsx](src/App.tsx)` `useRaycastedFlashes`; `[src/LabeledSwitch.tsx](src/LabeledSwitch.tsx)` ray-cast toggle; `[src/FlashExposureOverlay.tsx](src/FlashExposureOverlay.tsx)`; `[src/flashExposure.ts](src/flashExposure.ts)` grid + ray pipeline, `solidGridCache`; `[src/grenadeEffects.ts](src/grenadeEffects.ts)` flash radii helpers.      |
| **Flash drag performance**                    | While dragging a flash with ray-cast on, show **radial** preview only; recomputes expensive overlay **once on release**.                                                                     | `[src/App.tsx](src/App.tsx)` `GrenadeEffectRadius` + `useSimpleFlashWhileDragging`; ties to `draggingGrenadeId`.                                                                                                                                                                     |
| **Per-map annotation history**                | Each map has independent strokes and grenades with **undo / redo** stacks.                                                                                                                   | `[src/App.tsx](src/App.tsx)` `annotationHistories`, `HistoryAction` (`add`, `remove`, `moveGrenade`), keyboard shortcuts.                                                                                                                                                            |
| **Clear actions**                             | Clear all ink or all grenades on the current map (separate buttons).                                                                                                                         | `[src/App.tsx](src/App.tsx)` `clearDrawing`, `clearGrenades`, `removeCurrentMapAnnotations`; `[src/App.css](src/App.css)` mobile `.tools-panel .clear-actions` (`flex-shrink: 0` so the segment does not collapse in the capped tools column).                                                                                                 |
| **Viewport & zoom**                           | Fit map to workspace; optional manual zoom / pan; wheel zoom; middle-mouse pan.                                                                                                              | `[src/App.tsx](src/App.tsx)` `mapView`, `useStageSize`, `handleWheel`, pan gesture ref, `clampZoom`, `NAVIGATION_GESTURE_LOCK`; `[src/App.css](src/App.css)` stage layout.                                                                                                           |
| **Buy zones overlay**                         | Optional T/CT buy overlay image per map.                                                                                                                                                     | `[src/App.tsx](src/App.tsx)` `showBuyZones` + `buyZonesOverlayImage`; `[src/LabeledSwitch.tsx](src/LabeledSwitch.tsx)` buy-zone toggle; `[src/mapCatalog.ts](src/mapCatalog.ts)` overlay src.                                                                                                                                                          |
| **Mobile general panel**                      | Narrow viewports: eyebrow + map toggle in the top bar; opening the map list shows a fullscreen sheet (creator note + buy-zone toggle only there) over the canvas and tools.                  | `[src/App.css](src/App.css)` `@media (max-width: 768px)`, `.mobile-map-picker-sheet-open`; `[src/App.tsx](src/App.tsx)` `general-panel`, `isMapPickerOpen`, body scroll lock.                                                                                                       |
| **Tool UX (shortcuts & help)**                | Keyboard tool switching; grenade radius help via tooltips (portal, hover/focus/mobile pin behavior).                                                                                         | `[src/App.tsx](src/App.tsx)` `TOOL_SHORTCUTS`, `GrenadeUtilityToolCell`, `createPortal`; `[src/App.css](src/App.css)` tooltip/layout; `[src/grenadeEffects.ts](src/grenadeEffects.ts)` `getGrenadeHelpText`.                                                                         |
| **Accessibility & cursors**                   | Crosshair for utility placement; hidden system cursor + DOM brush dot for ink; grab/grabbing on grenade handle; canvas cursor updates must touch **all** stage canvases.                     | `[src/App.tsx](src/App.tsx)` `brushCursorRef`, `setKonvaCanvasesCursor`, `restoreKonvaCanvasCursor`, `stageClassName`.                                                                                                                                                               |
| **Konva layer ordering**                      | Map tiles and overlays below committed annotations; **live ink layer** above committed lines so strokes do not flicker under the pointer.                                                    | `[src/App.tsx](src/App.tsx)` `Layer` order and `@ink:konva` comments; annotations `listening` vs ink `listening={false}` on strokes.                                                                                                                                                 |


### How to maintain this table

1. **New feature:** Add a row with a clear **user-facing name**, **why it exists**, and **every module** that implements or constrains it (especially `App.tsx` subsystems — search `@ink:`* in touched trees).
2. **Behavior change:** Update the intent column if user value shifts; update couplings when files/handlers/move.
3. **Removal:** Delete the row or mark deprecated only if product explicitly removes the capability—avoid silent deletion.
4. **Refactors:** If logic moves files, update coupling paths; run through the regression lens: overlap rules, Stage vs shape events, flash drag preview, undo shapes.

